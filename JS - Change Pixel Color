//Try It! Using For Loops
//Create new 200x200 image and then change each pixel to another color (ex is from black to yellow)

var img = new SimpleImage(200,200);
for (var pixel of img.values()){
    var newR = 255;
    var newG = 255;
    pixel.setRed(newR);
    pixel.setGreen(newG);
}
print(img)
